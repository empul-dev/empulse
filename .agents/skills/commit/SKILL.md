# Commit Skill
1. Run all relevant tests for changed files
2. Run linters (python: ruff check, typescript: eslint)
3. Show the full diff of changed files, then stage specific files by name with `git add`
4. Write a conventional commit message summarizing changes
5. Commit with `git commit`
