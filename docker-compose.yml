services:
  emtulli:
    build: .
    ports:
      - "8189:8189"
    volumes:
      - emtulli-data:/app/data
    environment:
      - EMBY_URL=${EMBY_URL}
      - EMBY_API_KEY=${EMBY_API_KEY}
      - DB_PATH=/app/data/emtulli.db
    restart: unless-stopped

volumes:
  emtulli-data:
