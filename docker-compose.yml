services:
  empulse:
    build: .
    ports:
      - "8189:8189"
    volumes:
      - empulse-data:/app/data
    environment:
      - EMBY_URL=${EMBY_URL}
      - EMBY_API_KEY=${EMBY_API_KEY}
      - DB_PATH=/app/data/empulse.db
    restart: unless-stopped

volumes:
  empulse-data:
